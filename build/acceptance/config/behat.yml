default:
  autoload:
    '': %paths.base%/../features/bootstrap
  suites:
    default:
      paths:
        - %paths.base%/../features
      contexts:
        - FeatureContext
  extensions:
    Behat\MinkExtension:
      sessions:
        default:
          selenium2: ~
