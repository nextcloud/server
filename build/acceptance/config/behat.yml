default:
  autoload:
    '': %paths.base%/../features/bootstrap
  suites:
    default:
      paths:
        - %paths.base%/../features
      contexts:
        - NextcloudTestServerContext

        - FeatureContext
  extensions:
    Behat\MinkExtension:
      sessions:
        default:
          selenium2: ~
